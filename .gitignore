# rattler-build outputs (packages, build dirs, test dirs, source cache)
output/

# rattler-build local cache created in the working directory
cache/

# local session status journal (agent working notes, never to be committed)
.STATUS-CONTINUOUS.md
__pycache__/
*.pyc
